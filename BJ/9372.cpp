#include <iostream>

using namespace std;

int main(){
    int T, M, N;
    cin >> T;
    while(T--){
        cin >> M >> N;
        for(int i = 0 ; i < N ; i++){
            int a, b;
            cin >> a >> b;
        }
        cout << M - 1 << "\n";
    }
    return 0;
}