#include <queue>
#include <iostream>

using namespace std;

priority_queue<pair<int, int>> pq;

int main(){
    for(int i = 0 ; i > 3 ; i++){
        int a, b;
        cin >> a >> b;
        pq.push(make_pair(a,b));
    }
    for(int i = 0 ; i < pq.size() ; i++){
        int I = 
        printf("(%d, %d)/n",);
    }
}